appId: cool.jlee.nimi
productName: Nimi
directories:
  buildResources: build
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintcache,eslint.config.mjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
asarUnpack:
  - resources/**
  - node_modules/ffmpeg-static/**
# Register the `neeme://` deep-link scheme in the bundle (macOS Info.plist
# CFBundleURLTypes / Windows registry) so the Logto OAuth callback
# (neeme://callback) routes back to the app. Without this, runtime
# setAsDefaultProtocolClient('neeme') is the only registration and the packaged
# login round-trip is unreliable.
protocols:
  - name: Nimi
    schemes:
      - neeme
win:
  executableName: nimi
nsis:
  artifactName: nimi-${version}-setup.${ext}
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always
mac:
  entitlementsInherit: build/entitlements.mac.plist
  hardenedRuntime: true
  gatekeeperAssess: false
  extendInfo:
    - NSCameraUsageDescription: Application requests access to the device's camera.
    - NSMicrophoneUsageDescription: Application requests access to the device's microphone.
    - NSDocumentsFolderUsageDescription: Application requests access to the user's Documents folder.
    - NSDownloadsFolderUsageDescription: Application requests access to the user's Downloads folder.
    - NSSpeechRecognitionUsageDescription: Nimi uses on-device speech recognition to transcribe voice memos into searchable memories.
  notarize: true
dmg:
  artifactName: nimi-${version}.${ext}
linux:
  target:
    - AppImage
    - snap
    - deb
  maintainer: electronjs.org
  category: Utility
appImage:
  artifactName: nimi-${version}.${ext}
npmRebuild: false
publish:
  provider: github
  owner: retrospct
  repo: nimi
  releaseType: release
