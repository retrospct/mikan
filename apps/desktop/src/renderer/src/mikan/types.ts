// types.ts — small shared UI types for the Mikan components.

// The animated states the Mikan diamond mark can be in.
export type MikanMarkState = 'idle' | 'thinking' | 'gathering' | 'drafting' | 'happy' | 'done'
