// import { initTRPC } from '@trpc/server'
// import { z } from 'zod'
// // import SuperJSON from 'superjson';
// // TODO: Implement SuperJSON, update /web/app/layout.tsx
// // And match it in the client setup (httpBatchLink above).
// const t = initTRPC.create()
// // const t = initTRPC.create({ transformer: SuperJSON });

// export const appRouter = t.router({
//   getUserStats: t.procedure.input(z.object({ userId: z.string() })).query(({ input }) => ({
//     userId: input.userId,
//     followers: 123,
//     posts: 42
//   }))
// })

// export type AppRouter = typeof appRouter

export {}
