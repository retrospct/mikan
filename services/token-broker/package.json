{
  "name": "@mikan/token-broker",
  "version": "1.5.0",
  "description": "Logto → Turso token broker: JWKS-verify the Logto access token and mint a short-lived, DB-scoped Turso token for the user's personal embedded replica.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "jose": "^6.2.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.23.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
