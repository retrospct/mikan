// types.ts — small shared UI types for the Neeme components.

// The animated states the Neeme diamond mark can be in.
export type NeemeMarkState = 'idle' | 'thinking' | 'gathering' | 'drafting' | 'happy' | 'done'
